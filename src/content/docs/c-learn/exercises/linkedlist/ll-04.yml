id: ll-04
title: "环形链表检测"
difficulty: medium
tags:
  - "链表"
  - "快慢指针"
  - "Floyd"
description_md: |
  ## 背景故事
  
  链表是入门数据结构的第一课。数组在内存中是连续排列的（像电影院的连座），链表通过指针串联（像寻宝游戏中的线索纸条）。链表让你理解数据在内存中不一定是连续的这个重要概念，是理解所有高级数据结构的基础。
  
  ## 题目描述
  
  用快慢指针(Fast=Slow检测)判断链表是否有环。输入链表建立方式，输出 YES/NO。
  
  **Tags: 链表, 快慢指针, Floyd** | **Difficulty: 中等**
  
  ## 关键概念
  
  ### 链表
  链表由节点组成，每个节点有数据和指向下一个节点的指针。优点：动态增长、快速插入删除。缺点：不能随机访问（必须从头遍历）。
  
  ## 输入格式
  
  仔细阅读下面的示例，确定输入数据的格式。
  
  ## 输出格式
  
  按照题目要求输出结果。注意输出格式必须与示例完全一致（包括空格和换行）。
  
  ## 示例
  
  | 输入 | 输出 | 解释 |
  |------|------|------|
  | `4\n1 2 3 4\n0\n` | `NO\n` | Sample 1 |
  | `4\n1 2 3 4\n2\n` | `YES\n` | Sample 2 |
  
  ## 数据范围与约束
  
  - 难度等级：中等
  - 请考虑边界情况：零值、负数、最小值、最大值等
  - 时间限制：1 秒 | 内存限制：128 MB
  
  ## 提示
  
  1. slow每次走1步, fast每次走2步
  2. 相遇则有环
  3. 第一步：先在纸上画出你的思路或算法流程。
  4. 第二步：确定边界情况——空输入、最大值、负数等怎么处理？
  5. 第三步：选择合适的数据结构——数组？指针？结构体？
  6. 第四步：用示例中的测试数据验证你的程序。
  7. 提示：开发时可以用 printf 打印中间变量来调试逻辑。
  8. 效率提示：注意算法的时间复杂度，避免不必要的嵌套循环。
  
template_code: |
  #include <stdio.h>
  #include <stdlib.h>
  
  int main() {
      // Write your code here
      return 0;
  }
test_cases:
  - input: "4
1 2 3 4
0
"
    expected_output: "NO
"
  - input: "4
1 2 3 4
2
"
    expected_output: "YES
"
hints:
  - "slow每次走1步, fast每次走2步"
  - "相遇则有环"
  - "第一步：先在纸上画出你的思路或算法流程。"
  - "第二步：确定边界情况——空输入、最大值、负数等怎么处理？"
  - "第三步：选择合适的数据结构——数组？指针？结构体？"
  - "第四步：用示例中的测试数据验证你的程序。"
  - "提示：开发时可以用 printf 打印中间变量来调试逻辑。"
  - "效率提示：注意算法的时间复杂度，避免不必要的嵌套循环。"
solution_code: |
  int hasCycle(Nd *h){
      Nd *s=h, *f=h;
      while(f && f->n){ s=s->n; f=f->n->n; if(s==f) return 1; }
      return 0;
  }
