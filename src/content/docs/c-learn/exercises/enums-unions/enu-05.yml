id: enu-05
title: "颜色枚举混合"
difficulty: easy
tags:
  - "enum"
  - "位或"
  - "颜色"
description_md: |
  ## 背景故事
  
  枚举和联合体是 C 语言中容易被忽视但非常有用的特性。枚举让代码更可读（用 RED 代替 1），联合体让你用不同方式解释同一块内存。理解它们能帮你读懂操作系统源码和网络协议实现。
  
  ## 题目描述
  
  定义枚举 Color {RED=1, GREEN=2, BLUE=4, YELLOW=3, CYAN=6, MAGENTA=5, WHITE=7}。输入一个数字(1-7)，输出包含哪些基础颜色。如 3 → 'Red+Green'。
  
  **Tags: enum, 位或, 颜色** | **Difficulty: 简单**
  
  ## 关键概念
  
  ### 基础语法
  这道题练习 C 语言的基础语法。如果不熟悉某个知识点，可以回到 Learn 模式查看对应的知识模块。
  
  ## 输入格式
  
  仔细阅读下面的示例，确定输入数据的格式。
  
  ## 输出格式
  
  按照题目要求输出结果。注意输出格式必须与示例完全一致（包括空格和换行）。
  
  ## 示例
  
  | 输入 | 输出 | 解释 |
  |------|------|------|
  | `3\n` | `Red+Green\n` | Sample 1 |
  | `5\n` | `Red+Blue\n` | Sample 2 |
  | `7\n` | `Red+Green+Blue\n` | Sample 3 |
  
  ## 数据范围与约束
  
  - 难度等级：简单
  - 请考虑边界情况：零值、负数、最小值、最大值等
  - 时间限制：1 秒 | 内存限制：128 MB
  
  ## 提示
  
  1. c & RED 判断是否包含红色
  2. 用 if 逐位检测
  3. 第一步：仔细读题，明确输入什么、输出什么。
  4. 第二步：声明变量，选对类型——整数用 int，小数用 float 或 double。
  5. 第三步：用 scanf 读入数据，处理后用 printf 输出结果。
  6. 常见错误：scanf 中忘记写 & 符号，如 scanf("%d", &x) 不能写成 scanf("%d", x)。
  7. 常见错误：判断相等用 ==（两个等号），一个等号 = 是赋值。
  8. 测试技巧：尝试不同的输入值验证你的程序是否正确。
  
template_code: |
  #include <stdio.h>
  
  int main() {
      // Write your code here
      return 0;
  }
test_cases:
  - input: "3
"
    expected_output: "Red+Green
"
  - input: "5
"
    expected_output: "Red+Blue
"
  - input: "7
"
    expected_output: "Red+Green+Blue
"
hints:
  - "c & RED 判断是否包含红色"
  - "用 if 逐位检测"
  - "第一步：仔细读题，明确输入什么、输出什么。"
  - "第二步：声明变量，选对类型——整数用 int，小数用 float 或 double。"
  - "第三步：用 scanf 读入数据，处理后用 printf 输出结果。"
  - "常见错误：scanf 中忘记写 & 符号，如 scanf('%d', &x) 不能写成 scanf('%d', x)。"
  - "常见错误：判断相等用 ==（两个等号），一个等号 = 是赋值。"
  - "测试技巧：尝试不同的输入值验证你的程序是否正确。"
solution_code: |
  int main(){
      enum{ R=1,G=2,B=4 };
      int c; scanf("%d",&c);
      if(c&R){ printf("Red"); if(c&(G|B)) printf("+"); }
      if(c&G){ printf("Green"); if(c&B) printf("+"); }
      if(c&B) printf("Blue");
      printf("\n"); return 0;
  }
