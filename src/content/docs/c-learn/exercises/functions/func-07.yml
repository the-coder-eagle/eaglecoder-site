id: func-07
title: "字符串转整数"
difficulty: medium
tags:
  - "函数"
  - "字符串"
  - "转换"
description_md: |
  ## 背景故事
  
  函数是代码复用的基础。你不会想每次打印输出都重写一遍 printf 的代码吧？把常用的操作打包成函数，一次编写，到处调用。函数让代码变得模块化、可测试、可维护。专业程序员写代码就像搭积木——每个函数是一块积木。
  
  ## 题目描述
  
  编写函数 `int my_atoi(char *s)`，将数字字符串转为整数（不处理负数）。不用 atoi 库函数。
  
  **Tags: 函数, 字符串, 转换** | **Difficulty: 中等**
  
  ## 关键概念
  
  ### 函数
  函数是一段有名字、可复用的代码。定义：`返回类型 函数名(参数) { ... return 值; }`。调用：`函数名(参数);`。
  
  ### 字符串
  C 语言没有字符串类型——字符串是字符数组，以 `\0`（空字符）结尾。`char s[100]` 最多存 99 个有效字符（留 1 个给 `\0`）。
  
  ## 输入格式
  
  仔细阅读下面的示例，确定输入数据的格式。
  
  ## 输出格式
  
  按照题目要求输出结果。注意输出格式必须与示例完全一致（包括空格和换行）。
  
  ## 示例
  
  | 输入 | 输出 | 解释 |
  |------|------|------|
  | `12345\n` | `12345\n` | Sample 1 |
  | `0\n` | `0\n` | Sample 2 |
  | `700800\n` | `700800\n` | Sample 3 |
  
  ## 数据范围与约束
  
  - 难度等级：中等
  - 请考虑边界情况：零值、负数、最小值、最大值等
  - 时间限制：1 秒 | 内存限制：128 MB
  
  ## 提示
  
  1. 遍历每个字符
  2. n = n*10 + (s[i]-'0')
  3. 第一步：先在纸上画出你的思路或算法流程。
  4. 第二步：确定边界情况——空输入、最大值、负数等怎么处理？
  5. 第三步：选择合适的数据结构——数组？指针？结构体？
  6. 第四步：用示例中的测试数据验证你的程序。
  7. 提示：开发时可以用 printf 打印中间变量来调试逻辑。
  8. 效率提示：注意算法的时间复杂度，避免不必要的嵌套循环。
  
template_code: |
  #include <stdio.h>
  
  int main() {
      // Write your code here
      return 0;
  }
test_cases:
  - input: "12345
"
    expected_output: "12345
"
  - input: "0
"
    expected_output: "0
"
  - input: "700800
"
    expected_output: "700800
"
hints:
  - "遍历每个字符"
  - "n = n*10 + (s[i]-'0')"
  - "第一步：先在纸上画出你的思路或算法流程。"
  - "第二步：确定边界情况——空输入、最大值、负数等怎么处理？"
  - "第三步：选择合适的数据结构——数组？指针？结构体？"
  - "第四步：用示例中的测试数据验证你的程序。"
  - "提示：开发时可以用 printf 打印中间变量来调试逻辑。"
  - "效率提示：注意算法的时间复杂度，避免不必要的嵌套循环。"
solution_code: |
  int my_atoi(char *s) {
      int r = 0;
      for (int i = 0; s[i]; i++)
          r = r * 10 + (s[i] - '0');
      return r;
  }
