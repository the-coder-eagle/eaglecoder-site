id: rec-03
title: "斐波那契递归"
difficulty: easy
tags:
  - "递归"
  - "斐波那契"
  - "树形递归"
description_md: |
  ## 背景故事
  
  递归是一种函数调用自己的编程技巧。它优雅地解决了很多分而治之的问题：把大问题分解成小问题，直到小到可以直接解决。虽然初学者觉得递归绕脑子，但一旦理解，你会发现它是描述问题最自然的方式。
  
  ## 题目描述
  
  用递归实现 `int fib(int n)`。输入 N (1≤N≤30)，输出 F(N)。打印递归调用次数（全局变量计数）。
  
  **Tags: 递归, 斐波那契, 树形递归** | **Difficulty: 简单**
  
  ## 关键概念
  
  ### 递归
  递归是函数自己调用自己。必须要有终止条件，否则无限递归直到栈溢出。递归三要素：终止条件、缩小规模、调用自身。
  
  ## 输入格式
  
  仔细阅读下面的示例，确定输入数据的格式。
  
  ## 输出格式
  
  按照题目要求输出结果。注意输出格式必须与示例完全一致（包括空格和换行）。
  
  ## 示例
  
  | 输入 | 输出 | 解释 |
  |------|------|------|
  | `5\n` | `F(5)=5 calls=15\n` | Sample 1 |
  | `10\n` | `F(10)=55 calls=109\n` | Sample 2 |
  
  ## 数据范围与约束
  
  - 难度等级：简单
  - 请考虑边界情况：零值、负数、最小值、最大值等
  - 时间限制：1 秒 | 内存限制：128 MB
  
  ## 提示
  
  1. fib(n) = fib(n-1) + fib(n-2)
  2. fib(1)=fib(2)=1
  3. 第一步：仔细读题，明确输入什么、输出什么。
  4. 第二步：声明变量，选对类型——整数用 int，小数用 float 或 double。
  5. 第三步：用 scanf 读入数据，处理后用 printf 输出结果。
  6. 常见错误：scanf 中忘记写 & 符号，如 scanf("%d", &x) 不能写成 scanf("%d", x)。
  7. 常见错误：判断相等用 ==（两个等号），一个等号 = 是赋值。
  8. 测试技巧：尝试不同的输入值验证你的程序是否正确。
  
template_code: |
  #include <stdio.h>
  
  int main() {
      // Write your code here
      return 0;
  }
test_cases:
  - input: "5
"
    expected_output: "F(5)=5 calls=15
"
  - input: "10
"
    expected_output: "F(10)=55 calls=109
"
hints:
  - "fib(n) = fib(n-1) + fib(n-2)"
  - "fib(1)=fib(2)=1"
  - "第一步：仔细读题，明确输入什么、输出什么。"
  - "第二步：声明变量，选对类型——整数用 int，小数用 float 或 double。"
  - "第三步：用 scanf 读入数据，处理后用 printf 输出结果。"
  - "常见错误：scanf 中忘记写 & 符号，如 scanf('%d', &x) 不能写成 scanf('%d', x)。"
  - "常见错误：判断相等用 ==（两个等号），一个等号 = 是赋值。"
  - "测试技巧：尝试不同的输入值验证你的程序是否正确。"
solution_code: |
  int fib(int n){
      if(n<=2) return 1;
      return fib(n-1)+fib(n-2);
  }
