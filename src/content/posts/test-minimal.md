---
title: "[测试] 无标签 & 极简字段"
description: "这篇文章没有标签、没有更新时间、没有阅读时间"
publishedAt: 2026-04-01
---

## 边界测试

这篇文章测试：

- `tags` 字段不存在 → 应显示为空标签区
- `updatedAt` 不存在 → 不应显示"更新于"
- `readingTime` 不存在 → 不应显示阅读时间

如果你看到这篇文章，说明缺失可选字段的处理是正确的。
